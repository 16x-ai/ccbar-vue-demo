# Token 代理地址，不在设置页展示。API 主机 / KEY / SECRET 在页面设置里填写。
VITE_TOKEN_ENDPOINT=/api/webphone-token
# TOKEN_PORT=3000
# WEBPHONE_PROXY_TARGET=https://call-ng.innopaas.com
# 仅 Vite 开发服务器读取；按需设置测试后端 origin。
# WEBPHONE_PROXY_TARGET=https://test.example.com
